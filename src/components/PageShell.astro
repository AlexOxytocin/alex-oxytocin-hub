---
import type { Locale, SectionRouteId } from '../config/routes';
import PlaceholderPage from './pages/PlaceholderPage.astro';

interface Props {
  locale: Locale;
  route: SectionRouteId;
}

const { locale, route } = Astro.props;
---

<PlaceholderPage locale={locale} route={route} />
