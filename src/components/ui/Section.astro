---
interface Props {
  class?: string;
  labelledby?: string;
  id?: string;
}

const { class: className, labelledby, id } = Astro.props;
---

<section id={id} class:list={['section', className]} aria-labelledby={labelledby}>
  <slot />
</section>
